### 经典场景：

1、简单 KV set get

2、原子性 事务支持 将多个redis命令合一起

3、库存秒杀 CAS乐观锁



现在CRedis已经支持TRocks相关功能

我们使用CRedis的客户端进行操作，CRedis的客户端支持同步和异步两种操作方式





## Redis原理

### 数据结构

1、动态字符串 SDS

要知道Redis里字符串里用的还是挺多的，然后Redis又是C语言写的嘛

但是C语言字符串存在很多问题：

比如获取字符串长度需要运算，非二进制安全（末尾有结束符），不可修改

Redis构建了简单动态字符串SDS作为String的底层实现



SDS结构体 C语言struct

元数据：保存 申请空间、使用空间、头类型

SDS具备动态扩容的能力，申请内存空间：

- 如果新字符串小于1M 申请两倍+1
- 如果大于1M 申请扩容后空间+1M+1



这样好处在于二进制安全，因为你的头信息指明了使用到的长度，此外方便获取字符串长度

并且减少内存分配次数，有预分配功能



2、Dict字典

因为Redis其实是一个键值对的数据库，我们通过Dict去实现键与值的映射关系，

那么Dict的实现主要是哈希表，然后每个哈希节点保存了key、value、指针

其实底层原理是很类似于hashmap的



3、RedisObject

在Redis里 任意数据类型的键和值都会被封装为一个RedisObject，也叫做Redis对象，所以在Dict的哈希节点，保存的只是key、value的指针

那么为什么要做这一层封装呢？我们知道Redis是通过dict来维护一个database内的映射关系的，而dict的key是字符串，固定用sds来表示就可以了，但是value的类型有很多，为了在dict能存储不同类型，封装了一层RedisObject

这个RedisObject内，元信息有对象类型（五种基本）、底层编码、lru、引用计数



### 网络模型

首先要理解用户态和内核态，注意这里以linux操作系统讲述，因为Redis也没有提供window的安装包

那么对于linux为内核的操作系统来说，我们的应用是没法直接访问硬件的，所以我们要借助内核的能力去访问一些系统的能力

但是内核本身来说也是一个应用，它也需要内存、cpu等资源，而用户应用也需要，所以我们需要将用户和内核隔离开，于是将进程的寻址空间划分成内核空间、用户空间

**寻址空间**

应用程序和内核都不能直接访问物理内存，而是分配一些虚拟内存映射到物理内存，程序通过虚拟地址访问

用户空间只能执行一些受限的指令，必须通过内核的接口来访问内核空间可以执行特权命令，此时需要在用户态和内核态进行切换

linux系统为了提高IO效率，会在用户空间和内核空间都加入缓冲区，写数据时将用户缓存数据拷贝到内核缓冲区然后写入设备，读数据时从设备读到内核缓冲区，再拷贝到用户缓冲区



**IO模型**

在UNIX网络编程归纳了五种IO模型

本质用户空间读取数据其实两个过程

1、等待数据就绪

2、从内核缓冲区把数据拷贝到用户缓冲区



FD：文件描述符 用来关联linux内的一个文件 因为在linux里有个设计理念叫万物皆文件

而所谓的IO多路复用本质是利用一个线程监听多个FD，在某个FD的IO事件就绪时通知，避免无效的阻塞等待，从而充分利用CPU资源

过程：

用户进程系统调用函数，指定监听FD集合，同时阻塞等待

然后就绪时将数据拷贝到用户空间

IO多路复用可以保证 读取数据时数据一定存在

常见的实现方式有：

- select
- poll
- epoll

三者主要区别是：

select poll在数据就绪后，会把监听的整个FD都返回，这样我们还需要再去遍历FD列表看哪些是准备好的，从时间复杂度角度上看是On

而epoll这种方式 内核准备好了后会把准备好的数据都返回 省去了遍历的操作 O1 常数级别



具体聊聊epoll的多路复用实现：

维护了一个红黑树保存要监听的FD，有一个链表维护就绪的FD

1、基于红黑树保存要监听的FD，保证即使监听的FD数量很多 增删改查效率都非常高

2、通过callback回调的机制监听红黑树上的FD 一旦就绪加入链表

3、epoll返回的仅仅是链表，即就绪的FD，这样用户进程就不需要再去遍历一遍，而select、poll其实返回的是FD整个数组，你还是需要去遍历一遍 看其中哪些就绪





**Redis是单线程还是多线程**

结论是 如果是Redis的核心业务部分（命令处理），答案是单线程

如果是整个Redis，那么是多线程，这里就有两个关键节点

- 4.0V 引入多线程异步处理一些耗时较久的任务 例如大Key的删除
- 6.0V 在核心网络模型引入多线程 进一步提高对多核CPU的效率

为什么？因为Redis的性能瓶颈是网络延迟而不是执行速度，多线程首先是导致过多的上下文切换带来不必要的开销，其次还会带来线程安全问题



Redis的网络模型的本质是基于epoll的IO多路复用+事件派发这种机制，当有对应的IO事件发生，例如客户端请求连接、或是连接建立后请求命令执行，或是Redis执行完后任务的写回，当FD就绪后会派发给响应的处理器进行处理。

以客户端连接为例，当多路复用程序发现server socket有可读事件，说明是客户端请求连接，会交给连接应答处理器，它会接收客户端socket，注册客户端FD到多路复用程序，这样这个客户端后续的执行命令请求会被多路复用程序给监听到





### **通信协议**

Redis是C/S架构，服务端和客户端的通信走RESP协议

这是Redis基于TCP协议设计出的，协议非常简单高效

这种简单高效的通信协议很方便Redis快速读取、解析命令，方便Redis能快速处理和响应大量的请求



### **内存管理**

注意Redis是基于内存存储，然而单节点的Redis内存不宜过大，会影响持久化和主从同步性能

我们可以修改配置文件设置Redis最大内存，然后Redis有两大内存回收机制



1、内存过期

即通过expire命令给Redis的key设置过期时间，Redis对于这类设置了TTL的key有两种删除策略

一是访问一个key时检查该key的存活时间，如果过期才删除

二是周期性抽样部分key，过期删除

如果实时删除的话，那需要对每个key设置定时任务，对cpu压力太大



2、内存淘汰

有时候Redis的key还是很多，需要主动挑选部分key删除

有八种策略

random、lru、lfu * TTL、ALL  = 6种 + 不淘汰、比较剩余TTL值

LRU是最少最近使用 LFU是最少频率使用

RedisObject里保存了相应的值为参考



### 集群

单节点的Redis并发能力也是有限的，因此需要进一步提高Redis的并发能力，就需要搭建主从集群，实现读写分离，此外多个节点能够有效提高可用性

Redis还提供了哨兵机制来实现主从集群的自动故障恢复

<img src="D:\note\工作文档\FrameWork\assets\image-20240702145757459.png" alt="image-20240702145757459" style="zoom: 33%;" />

可以看到哨兵承担了三个作用：

- 监控：sentinel会不断检查集群里master和slave节点的工作情况
- 自动故障恢复 如果master故障 会将一个slave提升为master
- 通知 sentinel充当Redis客户端的服务发现来源 集群发生故障转移时 会将最新信息推送到Redis客户端



**分片集群**

通常集群会搭配分布式，按照上述的架构方案，可以大幅提高读并发的能力，但面对海量数据的存储问题以及

高并发写的问题，使用分片集群来解决



分片集群特征：

- 集群中有多个master，每个master保存不同数据
- 每个master有多个slave节点
- master之间通过相互ping检测彼此健康状态
- 客户端请求可以访问集群任意节点，最终会被转发到正确节点上



redis key存储原理

会把每一个节点映射到0-16383个插槽上，数据key不与节点绑定，而是与插槽绑定，redis会根据key的有效部分计算插槽值，然后根据计算得到的插槽值和节点对应起来，简单来说就是通过插槽使得redis集群里的key与集群里的节点实现了解耦



### redis集群概念

1、分片 sharding

redis集群将数据分布在多个节点上，每个节点只存储整个数据集的一部分，这种数据分布方式称为分片

redis集群使用哈希槽来实现分片，整个key空间被划分为16384个哈希槽，redis集群根据key的哈希值将其映射到一个哈希槽

16384其实是2的14次方，这种设计使得hash计算和哈希槽的映射会非常高效，类似hashmap

使得slot有足够细的粒度来均匀分布数据



提一个hashmap里的点：hashmap的capacity总是2的幂

这样可以使用位运算符来实现高效的哈希计算和槽位映射

例如先通过key的hashcode()生成hash值

然后用（hash & capacity-1）来代替hash % capacity来快速得到桶的索引



2、节点 node

redis集群由多个Redis节点组成，每个节点可以是主节点master或是从节点slave

主节点负责读写、从节点负责复制主节点的数据，并在主节点故障时提供故障转移

主节点可以有多个，且每个主节点可以有一个或多个从节点



3、哈希槽 hash slot

每个主节点负责一定范围的hash slot

理论上一个slot可以放任意数量的key，它只是逻辑上的分片单位，将key分布到不同节点上



4、故障转移

master节点故障，集群会自动将其中一个从节点提升为新的主节点，保证高可用

该过程自动，由其他节点协同完成



5、重新分片

分片其实就是哈希槽和主节点映射关系重新分配的过程



限制：

在集群模式下，多键操作只能在同一个hash slot内的键上进行





## CRedis接入

申请集群

学习Credis如何保证原子性和秒杀时CAS乐观锁

本质上redis的使用是CRedis客户端和Redis服务端直接三次握手建立TCP连接访问

超时有以下几点

1、connect timeout 三次握手的超时

2、read timeout redis请求超时

3、Pool Wait Timeout 连接池等待时长 为了连接复用



### 集群类型介绍

#### 单机房缓存

在一个机房部署一个集群，单点故障可自动恢复（Sentinel）

#### 本机房部署

在每一个机房独立部署一个集群，不同机房的Redis集群无关联，用户机房就近读写

即事先利用距离做了一层路由

- 单点故障可自动恢复
- 机房内节点全挂吗，需要用户切流量到另外一侧机房
- 机房故障无需切换

#### XPipe单向同步

1. 多机房部署，只有一个Master，数据单向同步到其他机房
2. 用户写入主机房，读取支持多种路由模式（读主机房slave、就近机房读取）
3. 单点故障可自动恢复，机房内节点全挂，自动DR切换，机房故障自动DR切换
4. 支持海外机房数据同步（FRA法兰克福、SIN新加坡、ALI阿里云）、

#### XPipe双向同步

多机房部署，每个机房部署Master/Slave，数据双向同步，最终一致

用户机房就近读写



### Redis客户端

1、到redis/里去申请集群

2、引入framework-bom以及credis的依赖

3、使用credis客户端开发功能 

credis的客户端主要分同步&异步，这个还是比较好理解，但要特别注意异步的用法，千万不要刚get完一个Futrue后，就又马上获取Future结果，那和同步就没差别了



### 配置中心化

在qconfig里我们新建继承文件redis.common.properties，然后在继承的子文件中添加对应的配置项发布即可

要注意子文件里写法以及生效范围：

1、配置项=配置值 **应用粒度**

2、集群名+配置项 = 配置值 **集群粒度**

如果应用调用了多个redis集群，那么应用粒度都改变，集群只针对当前改变，并且集群小粒度优先



预热配置项：

warm.up.clusters：需要预热的集群名称，多个集群名用；分割

这个需要搭配pool.initialize.size使用（连接池初始化阶段 需要创建的连接数）

建议这个值不要超过8，因为在切换时应用实例会同时初始化连接，假设该值为8，然后你的应用实例数为50，那么应用实例在切换时瞬间创建400个连接，单个redis实例的连接上限是20000



### 事务支持

可以使用CacheProvider接口的execute(RedisAction action,String... watchKeys)

在RedisAction接口中调用需要在一个事务中被执行的命令，根据自身情况是否传入watchKeys数组





### QA

1、刚发布完有大量命令超时问题 即PoolWait

 这是由于系统刚发布时，和redis之间没有建立TCP连接，一开始调用的redis命令都要先建立TCP连接再执行命令，同时在建立的连接达到最大连接数后，后续命令不再建立新链接而是wait前面的连接还到连接池里或释放掉，这就是为什么会poolwait，运行一段时间后连接池会一直存有空闲连接，再调用redis命令就可以直接从连接池拿到连接执行命令，所以不会造成poolWait

----解决方案：可以在应用点火时预热好redis的连接



2、哈希算法

我们知道redis集群的key是通过哈希分到槽里的，credis里使用的是ketama_md5哈希算法





## ihub--李明冬

**CRedis简介**

credis是一个典型的服务端-客户端软件架构

服务端功能主要包括：Redis的管理（redis的分片对应实例等元信息）、高可用（单点故障自动处理）、DR切换（常见于大考场景 某个机房挂了 切换到另一个机房）、水平扩缩容（业务容量上升时 扩容 承接流量）

客户端功能：路由（客户端的命令怎么路由到具体的redis节点去执行）、动态配置（DR切换、水平扩缩容怎么把信息实时推送到客户端）

 

**集群搭建**

credis/上申请，申请重点关注的信息：

1、集群类型

目前credis有四种集群类型 如何选择？

<img src="D:\note\工作文档\FrameWork\assets\image-20240703185434425.png" alt="image-20240703185434425" style="zoom:50%;" />

如果跨IDC数据要强一致，那就需要设计到数据的同步、复制

XPipe有两种 单向同步和双向同步

双向同步的话使用的就不是原生的redis了 而是内部嵌入了一个CRD协议的改造版 这样每个机房的都

支持写入 而单向同步只有主机房支持写入，其他都是从节点只有读服务

XPipe双向同步的集群是又redis团队内部运维 单向同步的集群是DBA团队运维



2、使用方法

当DBA部署完Redis集群后，用户可以通过Credis client访问服务端，支持同步和异步两种方式使用

异步的方法适用于并发比较高性能较高的场景，调用的结果会通过回调Listener返回执行结果



同步与异步的选择？

1、QPS不高 特殊API（阻塞API） 应用代码

2、QPS较高 mget/sunion（批量命令/长耗时，不允许应用长时间阻塞）应用本身需要异步



本地开发模式--

意义？为了不依赖测试环境的集群，因为可能很多用户都去使用 如果每个都向测试集群写入会相互影响

这就需要用户在本地起一个redis服务：

需要再本地server.properties文件里配置env=local 将本地开发环境设置为Local

CRedis本地化配置：/opt/config/{appid}||framework/credis/集群名.properties

配置demo：group.master=127.0.0.1:6379

这样credis客户端就知道集群名初始化的节点



问题排查：

BAT Transaction：Redis

problem看异常 可以针对redis的错误异常 看机器 然后可以看到全链路的trace

clog：看CRedis.java.Error日志

这些其实属于是Credis的同事帮我们打的点，方便排查问题





**架构原理**

这里要先了解一下：

credis这个软件是框架团队做的，但是运维是DBA负责

这是大背景，然后双向同步类型的credis集群，其实不是原生的redis，FX团队改造过的，因此这个类型是框架团队内部负责维护



![image-20240703191316030](D:\note\工作文档\FrameWork\assets\image-20240703191316030.png)

config manager其实就是credis站点 上面保存了集群的一些元信息

Config Service是面向DBA运维人员，满足扩缩容的需求，为了实现自动化运维提供一整套接口



1、客户端是怎么通过一个集群名 即getProvider(ClusterName)获取到面向服务端的redis集群呢？

 通过http调用请求到Config Service获取信息 根据集群名+子环境查到所有节点下发到客户端 然后客户端根据这些信息生成redis cluster集群，为后续的访问和操作打下基础

这里再提一下子环境的概念，也是公司在推的，同一个集群名在不同的子环境下有不同的节点，主要是可以根据region分区或是group做子环境



2、客户端路由实现

其实路由有两个维度 一是路由到redis的哪个分片 二是路由到分片的哪个副本因为单机房内部每个分片有一主两从

路由到哪一个分片通过key做一致性hash确定分片，再根据客户端的读写配置来判断操作哪个节点

读写策略有（读写都master、如果qps较高可以读写分离）

确定了之后，就从客户端拿到这个节点的连接，然后发送命令，底层封装的是jedis



3、客户端动态变更实现

节点替换、扩缩容、主从切换等服务端节点出现变更的状况，当出现这些状况时如何实时推送到客户端？

发生变更时，其实依赖qconfig，qconfig上不保存元信息只保存版本信息，变更时会去更新qconfig上的版本信息，然后qconfig推送到客户端后，当发现版本变化后会主动向服务端拉取，然后做切换后续请求到新节点

从而使得变更实现自动化



总结：

服务端的变更客户端能够及时感知

客户端能根据key正确路由到对应节点，读取到数据



客户端问题---Redis热线

运维问题 扩容--Redis支持群 DBA介入









