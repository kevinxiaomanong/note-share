# 用户画像相关



## 分销uid需求

https://idev2.ctripcorp.com/issueDetail/DATAIS-2126



依赖上游表：dim_usr_alliance_uid（初始层）

清哥已经建了初始层的表：ctripdi_prodb.mid_dna_user_label_initial_8735

但我们发现这张初始层的表mid_dna_user_label_initial_8735过不了DQC校验

现在我们已经过了校验，然后我们有了14号的初始层分区

接下来的思路就是仿照8733标签写入sharein表的job，然后写8735 

要注意有跨天依赖、白名单、T端UID剔除



写入sharein表job：http://zeus.service.ops.ctripcorp.com/#/job/1427873 

（需要切换至share_di账号）



设置为依赖调度：依赖上游任务：http://zeus.service.ops.ctripcorp.com/#/preview/job/1423559

今天数据没出：是因为上游依赖的三个job只完成了两个 还在等待上游依赖运行



解读一下跨天依赖job：http://zeus.service.ops.ctripcorp.com/#/job/1423578

这是一个shell类型任务 依赖定时调度：每天0点5分执行

1423559

我们来理解一下为什么不对：



写入sharein的job依赖

即该任务为什么会一直卡注？

首先这个任务是一直依赖接入层job 但是



其他标签逻辑分析：

8605综合代理：

Full Join：全外连接，保留t1和t2中所有用户，确保二者都包含，且如果用户同时存在与t1和t2则合并字段（白名单优先）

问题1：http://zeus.service.ops.ctripcorp.com/#/job/1006443

这块我们不是往前一天的去写数据 为什么最后要通过随机函数将数据均匀分布到 256 个分区（与前面 `shuffle.partitions=256` 对应），避免数据倾斜，提升计算效率。

所以8605的剔除其实很简单：我们再初始层这个job 通过left join+where剔除掉即可



1073：http://zeus.service.ops.ctripcorp.com/#/job/344197

下面我们看看1073怎么去剔除分销

我们先比对一下1073的分销uid 和 8735标签的差异

![image-20250916202131630](D:\编程文档\note-share\项目类\assets\image-20250916202131630.png)

那么接下来就是看

![image-20250917141207731](D:\编程文档\note-share\项目类\assets\image-20250917141207731.png)

可以看到1073这个标签里有四种：



我们现在要把：8735里的uid（被误判成其他、内部专用账号、分销用户）移到分销里面

此外1037本身分销用户没被8735判断的 要移到其他里面



和业务方确认：

- 27686这些uid被划到其他
- 8735这些 79024647个uid 看是否被划进了1073其他里面 如果有的话移进来没有的话往分销用户里面新增



1073的初始层写入：

查userprofile_members_membertype_sourcefrom表然后匹配白名单

白名单里uid的账号类别是优先的 这点不变





我们新建zeus任务副本然后跑副本，得到了临时表tmp_sandbox_ctripdi_prodb.mid_dna_user_label_initial_1073



下面就是去验证数据了，我们要观察数据的变化看是否符合预期，清楚数据的变化

这里思考一个问题：如果这个标签被接口调用了，那其实你手动恢复是无效的，你最多补到对比层



hive to redis这个链路是有专门的shell脚本 它是定时调度 可以看下逻辑ctripdi/cdi/tools/dna-uid

然后分p3和p0两个队列





















1161用户星级：剔除分销uid

逻辑简单：直接LEFT Join 8735初始层表 然后+where条件剔除掉



8593 N合一聚合画像

也是直接LEFT JOIN



8649机票重要人士

也是直接LEFT JOIN剔除即可



## 数据同步Redis

zeus任务目录：ctripdi/cdi/tools/dna-uid下面





## NPS看板搭建

nova看板搭建：

口径：https://trip.larkenterprise.com/wiki/BmAYw2zd7itCUqkZEfvcCa12nrl

指标：https://trip.larkenterprise.com/wiki/HvywwTpqHinKLvkZEhgcx5uanLh

idev：https://idev2.ctripcorp.com/issueDetail/DATAIS-2076





### 口径梳理：

**用户规模**：

MUA&DAU:
表：ctripdi_prodb.adm_mau_data_mf

任务：http://zeus.service.ops.ctripcorp.com/#/job/1235741



性别占比：

临时表：tmp_ctripdi_prodb.tmp_371888_gender_distribution_june

任务（临时表）：http://zeus.service.ops.ctripcorp.com/#/file/371888



年龄分布：

临时表：tmp_371895_age_distribution_june

任务（临时表）：http://zeus.service.ops.ctripcorp.com/#/file/371895





1、如果是已有job 直接用结果表去配nova数据源上线

2、如果是file 临时表 我应该转换为job 然后再去配 如果写job 放在哪个目录

3、如果没有 我是不是应该仿照提供的口径 自己去写job



口径探明：

1、交易怎么判（统一订单表）

判状态



2、新客怎么算 （1046用户画像）



3、首单成交BU （1045用户画像）



问题：

1、如果我们的指标都是月维度

那计算用户180日交易流失率指标，比如我每月1号去调度一次

以9.1为例 其实是计算3月-8月这个期间交易流失率



























### 数仓模型：

分层：轻度聚合层+分析层

轻度聚合层：



先整合一个明细的大宽表出来：

访问记录表：





where d = 过去一个月 然后直接count(distinct) group 

group by d --->count 每天的日活

然后再group即可 





### 指标建设-访问类

#### 1、mau计算

ubt访问表+appid维表关联平台

group by plat、month、uid

外层group by platform+month count uid即可算出

union all

month

group by month+uid

外层group by month +count uid -->得到app+小程序的mau



```
SELECT month, plat, COUNT(uid) AS mau
FROM (
    SELECT a.uid, b.plat
        , SUBSTR(a.d, 1, 7) AS month
    FROM ctripdi_prodb.cdm_ubt_pageduration_di a
        INNER JOIN (
            SELECT app_id
                , CASE 
                    WHEN app_type IN ('微信小程序', '支付宝小程序') THEN '小程序'
                    WHEN app_name = '携程旅行'
                        AND app_type = '普通App'
                        AND appsource_type = 'ctrip'
                    THEN 'Ctrip APP'
                    ELSE '-'
                END AS plat
            FROM ctripdi_prodb.dim_mcd_app_id_df
            WHERE app_type IN ('微信小程序', '支付宝小程序')
                OR (app_name = '携程旅行'
                    AND app_type = '普通App'
                    AND appsource_type = 'ctrip')
        ) b
        ON a.app_id = b.app_id
    WHERE a.d BETWEEN '${zdt.add(2,-1).format("yyyy-MM-01")}' AND date_sub('${zdt.add(2,0).format("yyyy-MM-01")}', 1)
        AND DATEDIFF(a.d, from_unixtime(SUBSTRING(a.event_timestamp, 1, 10))) = 0
        AND a.page_duration > 0
        AND a.page_duration < 86400
        AND a.uid IS NOT NULL
        AND b.plat != '-'
    GROUP BY a.uid, b.plat, SUBSTR(a.d, 1, 7)
) t1
GROUP BY month, plat
UNION ALL
SELECT month, '小程序+appid' AS plat, COUNT(uid) AS mau
FROM (
    SELECT a.uid, SUBSTR(a.d, 1, 7) AS month
    FROM ctripdi_prodb.cdm_ubt_pageduration_di a
        INNER JOIN (
            SELECT app_id
            FROM ctripdi_prodb.dim_mcd_app_id_df
            WHERE app_type IN ('微信小程序', '支付宝小程序')
                OR (app_name = '携程旅行'
                    AND app_type = '普通App'
                    AND appsource_type = 'ctrip')
        ) b
        ON a.app_id = b.app_id
    WHERE a.d BETWEEN '${zdt.add(2,-1).format("yyyy-MM-01")}' AND date_sub('${zdt.add(2,0).format("yyyy-MM-01")}', 1)
        AND DATEDIFF(a.d, from_unixtime(SUBSTRING(a.event_timestamp, 1, 10))) = 0
        AND a.page_duration > 0
        AND a.page_duration < 86400
        AND a.uid IS NOT NULL
    GROUP BY a.uid, SUBSTR(a.d, 1, 7)
) t1
GROUP BY month
```

**这里可以替换zdt函数 去跑数据**

a.d BETWEEN '2024-08-01' AND '2024-08-31'



![image-20250927020459816](D:\编程文档\note-share\项目类\assets\image-20250927020459816.png)

这个数据和excel是对上的





#### 2、日均dau计算

我们要先基于d+uid+platform+month去group by 然后外层group by d+platform+month再count即得到每天每个平台的日活



再group platform+month 然后外层AVG 日活 即得到了某个月的平均日活 然后写入该月分区

month做分区字段



算整体的时候不对plat做聚合即可



```
SELECT month, plat, AVG(dau) AS dau_avg
FROM (
    SELECT month, plat, d, COUNT(uid) AS dau
    FROM (
        SELECT a.uid, b.plat, a.d
            , SUBSTR(a.d, 1, 7) AS month
        FROM ctripdi_prodb.cdm_ubt_pageduration_di a
            INNER JOIN (
                SELECT app_id
                    , CASE 
                        WHEN app_type IN ('微信小程序', '支付宝小程序') THEN '小程序'
                        WHEN app_name = '携程旅行'
                            AND app_type = '普通App'
                            AND appsource_type = 'ctrip'
                        THEN 'Ctrip APP'
                        ELSE '-'
                    END AS plat
                FROM ctripdi_prodb.dim_mcd_app_id_df
                WHERE app_type IN ('微信小程序', '支付宝小程序')
                    OR (app_name = '携程旅行'
                        AND app_type = '普通App'
                        AND appsource_type = 'ctrip')
            ) b
            ON a.app_id = b.app_id
        WHERE a.d BETWEEN '${zdt.add(2,-1).format("yyyy-MM-01")}' AND date_sub('${zdt.add(2,0).format("yyyy-MM-01")}', 1)
            AND DATEDIFF(a.d, from_unixtime(SUBSTRING(a.event_timestamp, 1, 10))) = 0
            AND a.page_duration > 0
            AND a.page_duration < 86400
            AND a.uid IS NOT NULL
            AND b.plat != '-'
        GROUP BY a.uid, a.d, b.plat, SUBSTR(a.d, 1, 7)
    ) t1
    GROUP BY d, plat, month
) t2
GROUP BY plat, month
UNION ALL
SELECT month, '小程序+app' AS plat, AVG(dau) AS dau_avg
FROM (
    SELECT month, d, COUNT(uid) AS dau
    FROM (
        SELECT a.uid, a.d
            , SUBSTR(a.d, 1, 7) AS month
        FROM ctripdi_prodb.cdm_ubt_pageduration_di a
            INNER JOIN (
                SELECT app_id
                FROM ctripdi_prodb.dim_mcd_app_id_df
                WHERE app_type IN ('微信小程序', '支付宝小程序')
                    OR (app_name = '携程旅行'
                        AND app_type = '普通App'
                        AND appsource_type = 'ctrip')
            ) b
            ON a.app_id = b.app_id
        WHERE a.d BETWEEN '${zdt.add(2,-1).format("yyyy-MM-01")}' AND date_sub('${zdt.add(2,0).format("yyyy-MM-01")}', 1)
            AND DATEDIFF(a.d, from_unixtime(SUBSTRING(a.event_timestamp, 1, 10))) = 0
            AND a.page_duration > 0
            AND a.page_duration < 86400
            AND a.uid IS NOT NULL
        GROUP BY a.uid, a.d, SUBSTR(a.d, 1, 7)
    ) t1
    GROUP BY d, month
) t2
GROUP BY month
```

同理

a.d BETWEEN '2024-08-01' AND '2024-08-31'

![image-20250927023620520](D:\编程文档\note-share\项目类\assets\image-20250927023620520.png)

#### 3、mau年龄段占比

我们先找到platfrom某月的所有uid

然后和sharein_di.edw_di_dna_user_label_1175关联得到年龄



根据年龄所处范围-->计算年龄段



得到明细之后 根据platform+月+年龄段去分组 算占比即可



这里计算占比可以使用 sum over(窗口函数的方式进行)



```
SELECT month, plat, age_group, COUNT(uid) AS user_cnt
    , ROUND(COUNT(uid) * 1.0 / SUM(COUNT(uid)) OVER (PARTITION BY month, plat ), 4) AS ratio
FROM (
    SELECT a.uid, b.plat
        , SUBSTR(a.d, 1, 7) AS month
        , CASE 
            WHEN u.label_value_text <= 18 THEN '[0,18]'
            WHEN u.label_value_text BETWEEN 19 AND 24 THEN '[19,24]'
            WHEN u.label_value_text BETWEEN 25 AND 30 THEN '[25,30]'
            WHEN u.label_value_text BETWEEN 31 AND 35 THEN '[31,35]'
            WHEN u.label_value_text BETWEEN 36 AND 40 THEN '[36,40]'
            WHEN u.label_value_text BETWEEN 41 AND 45 THEN '[41,45]'
            WHEN u.label_value_text BETWEEN 46 AND 50 THEN '[46,50]'
            WHEN u.label_value_text BETWEEN 51 AND 60 THEN '[51,60]'
            WHEN u.label_value_text > 60 THEN '60以上'
        END AS age_group
    FROM ctripdi_prodb.cdm_ubt_pageduration_di a
        INNER JOIN (
            SELECT app_id
                , CASE 
                    WHEN app_type IN ('微信小程序', '支付宝小程序') THEN '小程序'
                    WHEN app_name = '携程旅行'
                        AND app_type = '普通App'
                        AND appsource_type = 'ctrip'
                    THEN 'Ctrip APP'
                    ELSE '-'
                END AS plat
            FROM ctripdi_prodb.dim_mcd_app_id_df
            WHERE app_type IN ('微信小程序', '支付宝小程序')
                OR (app_name = '携程旅行'
                    AND app_type = '普通App'
                    AND appsource_type = 'ctrip')
        ) b
        ON a.app_id = b.app_id
        INNER JOIN sharein_di.edw_di_dna_user_label_1175 u ON a.uid = u.uid
    WHERE a.d BETWEEN '2025-07-01' AND '2025-07-31'
        AND DATEDIFF(a.d, from_unixtime(SUBSTRING(a.event_timestamp, 1, 10))) = 0
        AND a.page_duration > 0
        AND a.page_duration < 86400
        AND a.uid IS NOT NULL
        AND b.plat != '-'
    GROUP BY a.uid, b.plat, SUBSTR(a.d, 1, 7), age_group
) t1
GROUP BY plat, month, age_group
UNION ALL
SELECT month, '小程序+app' AS plat, age_group, COUNT(uid) AS user_cnt
    , ROUND(COUNT(uid) * 1.0 / SUM(COUNT(uid)) OVER (PARTITION BY month ), 4) AS ratio
FROM (
    SELECT a.uid, SUBSTR(a.d, 1, 7) AS month
        , CASE 
            WHEN u.label_value_text <= 18 THEN '[0,18]'
            WHEN u.label_value_text BETWEEN 19 AND 24 THEN '[19,24]'
            WHEN u.label_value_text BETWEEN 25 AND 30 THEN '[25,30]'
            WHEN u.label_value_text BETWEEN 31 AND 35 THEN '[31,35]'
            WHEN u.label_value_text BETWEEN 36 AND 40 THEN '[36,40]'
            WHEN u.label_value_text BETWEEN 41 AND 45 THEN '[41,45]'
            WHEN u.label_value_text BETWEEN 46 AND 50 THEN '[46,50]'
            WHEN u.label_value_text BETWEEN 51 AND 60 THEN '[51,60]'
            WHEN u.label_value_text > 60 THEN '60以上'
        END AS age_group
    FROM ctripdi_prodb.cdm_ubt_pageduration_di a
        INNER JOIN (
            SELECT app_id
            FROM ctripdi_prodb.dim_mcd_app_id_df
            WHERE app_type IN ('微信小程序', '支付宝小程序')
                OR (app_name = '携程旅行'
                    AND app_type = '普通App'
                    AND appsource_type = 'ctrip')
        ) b
        ON a.app_id = b.app_id
        INNER JOIN sharein_di.edw_di_dna_user_label_1175 u ON a.uid = u.uid
    WHERE a.d BETWEEN '2025-07-01' AND '2025-07-31'
        AND DATEDIFF(a.d, from_unixtime(SUBSTRING(a.event_timestamp, 1, 10))) = 0
        AND a.page_duration > 0
        AND a.page_duration < 86400
        AND a.uid IS NOT NULL
    GROUP BY a.uid, SUBSTR(a.d, 1, 7), age_group
) t1
GROUP BY month, age_group
```

a.d BETWEEN '2025-07-01' AND '2025-07-31'

**这里要理解一个点：**



为什么我在plat, month, age_group的某个分区能拿到其他所有分区的uid计数之和？

---SUM(COUNT(uid)) OVER (PARTITION BY month, plat)

其实这得益于窗口函数实现每个分组占总体的比例，功能是在plat, month相同的所有记录中，计算这些记录的count（uid）总和

窗口函数会对**整个窗口内的所有数据**进行计算（这里是求和），并将结果 “广播” 到窗口内的每一条记录中。

即使你在`[0,18]`这个分组里，也能通过窗口函数拿到`[19,24]`、`[25,30]`等其他分组的用户数总和（即总用户数）。

如果使用普通的GROUP BY你需要先计算总用户数再关联回来，窗口函数的优势在于一次计算无需额外关联



这个差异稍微有点大，再比对看下



#### 4、mau的常住地分类

常住地分类有：一线、新一线、...

我们先通过uid 跟用户画像常住地的1013的sharein关联 得到cityId

再通过cityId去匹配

这个通过dim_hoteldb.dimcity_source表的pcitylevel字段



```
SELECT month, plat, pcitylevel, COUNT(uid) AS user_cnt
    , ROUND(COUNT(uid) * 1.0 / SUM(COUNT(uid)) OVER (PARTITION BY month, plat ), 4) AS ratio
FROM (
    SELECT a.uid, b.plat, c.pcitylevel
        , SUBSTR(a.d, 1, 7) AS month
    FROM ctripdi_prodb.cdm_ubt_pageduration_di a
        INNER JOIN (
            SELECT app_id
                , CASE 
                    WHEN app_type IN ('微信小程序', '支付宝小程序') THEN '小程序'
                    WHEN app_name = '携程旅行'
                        AND app_type = '普通App'
                        AND appsource_type = 'ctrip'
                    THEN 'Ctrip APP'
                    ELSE '-'
                END AS plat
            FROM ctripdi_prodb.dim_mcd_app_id_df
            WHERE app_type IN ('微信小程序', '支付宝小程序')
                OR (app_name = '携程旅行'
                    AND app_type = '普通App'
                    AND appsource_type = 'ctrip')
        ) b
        ON a.app_id = b.app_id
        INNER JOIN sharein_di.edw_di_dna_user_label_1013 u ON a.uid = u.uid
        INNER JOIN (
            SELECT city_id, pcitylevel
            FROM dim_hoteldb.dimcity_source
            WHERE d = date_sub('${zdt.add(2,0).format("yyyy-MM-01")}', 1)
                AND currentflag = 'T'
        ) c
        ON u.label_value_text = c.city_id
    WHERE a.d BETWEEN '${zdt.add(2,-1).format("yyyy-MM-01")}' AND date_sub('${zdt.add(2,0).format("yyyy-MM-01")}', 1)
        AND DATEDIFF(a.d, from_unixtime(SUBSTRING(a.event_timestamp, 1, 10))) = 0
        AND a.page_duration > 0
        AND a.page_duration < 86400
        AND a.uid IS NOT NULL
        AND b.plat != '-'
    GROUP BY a.uid, b.plat, SUBSTR(a.d, 1, 7), c.pcitylevel
) t1
GROUP BY plat, month, pcitylevel
UNION ALL
SELECT month, '小程序+APP' AS plat, pcitylevel, COUNT(uid) AS user_cnt
    , ROUND(COUNT(uid) * 1.0 / SUM(COUNT(uid)) OVER (PARTITION BY month ), 4) AS ratio
FROM (
    SELECT a.uid, c.pcitylevel
        , SUBSTR(a.d, 1, 7) AS month
    FROM ctripdi_prodb.cdm_ubt_pageduration_di a
        INNER JOIN (
            SELECT app_id
            FROM ctripdi_prodb.dim_mcd_app_id_df
            WHERE app_type IN ('微信小程序', '支付宝小程序')
                OR (app_name = '携程旅行'
                    AND app_type = '普通App'
                    AND appsource_type = 'ctrip')
        ) b
        ON a.app_id = b.app_id
        INNER JOIN sharein_di.edw_di_dna_user_label_1013 u ON a.uid = u.uid
        INNER JOIN (
            SELECT city_id, pcitylevel
            FROM dim_hoteldb.dimcity_source
            WHERE d = date_sub('${zdt.add(2,0).format("yyyy-MM-01")}', 1)
                AND currentflag = 'T'
        ) c
        ON u.label_value_text = c.city_id
    WHERE a.d BETWEEN '${zdt.add(2,-1).format("yyyy-MM-01")}' AND date_sub('${zdt.add(2,0).format("yyyy-MM-01")}', 1)
        AND DATEDIFF(a.d, from_unixtime(SUBSTRING(a.event_timestamp, 1, 10))) = 0
        AND a.page_duration > 0
        AND a.page_duration < 86400
        AND a.uid IS NOT NULL
    GROUP BY a.uid, SUBSTR(a.d, 1, 7), c.pcitylevel
) t1
GROUP BY month, pcitylevel
```



**解读一下zdt函数**

核心作用是根据当前时间计算相对日期，避免手动修改SQL中的日期

```
zdt.format(zdt.add(zdt.trunc(zdt.now(), 'MONTH'), -1), 'yyyy-MM-dd')
```



```
- 起始日期：查询月份的第一天
'${zdt.add(2,-1).format("yyyy-MM-01")}'

-- 结束日期：查询月份的最后一天
date_sub('${zdt.add(2,0).format("yyyy-MM-01")}', 1)
```

这样是为了在月末两天提前把数据计算出来，例如2025-08-30 加两天之后到09-01，这样时间范围就变成了

2025-08-01 -- 2025-08-31



#### 5、TOP10 城市占比动态

先拿明细 平台+月份+uid+城市



算各个城市的占比

group 



怎么得到城市这个字段信息

sharein_di.edw_di_dna_user_label_1013 u ON a.uid = u.uid

和1013关联

```
SELECT month, plat, city, cnt, ratio
    , city_rank
FROM (
    SELECT month, plat, city, cnt, ratio
        , ROW_NUMBER() OVER (PARTITION BY plat, month ORDER BY cnt DESC) AS city_rank
    FROM (
        SELECT month, plat, city, count(uid) AS cnt
            , round(count(uid) * 1.0 / sum(count(uid)) OVER (PARTITION BY month, plat ), 3) AS ratio
        FROM (
            SELECT t1.uid, t1.plat, t1.month, u.label_value_text AS city
            FROM (
                SELECT a.uid, b.plat
                    , SUBSTR(a.d, 1, 7) AS month
                FROM ctripdi_prodb.cdm_ubt_pageduration_di a
                    INNER JOIN (
                        SELECT app_id
                            , CASE 
                                WHEN app_type IN ('微信小程序', '支付宝小程序') THEN '小程序'
                                WHEN app_name = '携程旅行'
                                    AND app_type = '普通App'
                                    AND appsource_type = 'ctrip'
                                THEN 'Ctrip APP'
                                ELSE '-'
                            END AS plat
                        FROM ctripdi_prodb.dim_mcd_app_id_df
                        WHERE app_type IN ('微信小程序', '支付宝小程序')
                            OR (app_name = '携程旅行'
                                AND app_type = '普通App'
                                AND appsource_type = 'ctrip')
                    ) b
                    ON a.app_id = b.app_id
                WHERE a.d BETWEEN '2025-07-01' AND '2025-07-31'
                    AND DATEDIFF(a.d, from_unixtime(SUBSTRING(a.event_timestamp, 1, 10))) = 0
                    AND a.page_duration > 0
                    AND a.page_duration < 86400
                    AND a.uid IS NOT NULL
                    AND b.plat != '-'
                GROUP BY a.uid, b.plat, SUBSTR(a.d, 1, 7)
            ) t1
                INNER JOIN sharein_di.edw_di_dna_user_label_1013 u ON t1.uid = u.uid
        ) t2
        GROUP BY month, plat, city
    )
) t3
WHERE city_rank <= 10
UNION ALL
SELECT month, '小程序+app' AS plat, city, cnt, ratio
    , city_rank
FROM (
    SELECT month, city, cnt, ratio
        , ROW_NUMBER() OVER (PARTITION BY month ORDER BY cnt DESC) AS city_rank
    FROM (
        SELECT month, city, count(uid) AS cnt
            , round(count(uid) * 1.0 / sum(count(uid)) OVER (PARTITION BY month ), 3) AS ratio
        FROM (
            SELECT t1.uid, t1.month, u.label_value_text AS city
            FROM (
                SELECT a.uid, SUBSTR(a.d, 1, 7) AS month
                FROM ctripdi_prodb.cdm_ubt_pageduration_di a
                    INNER JOIN (
                        SELECT app_id
                        FROM ctripdi_prodb.dim_mcd_app_id_df
                        WHERE app_type IN ('微信小程序', '支付宝小程序')
                            OR (app_name = '携程旅行'
                                AND app_type = '普通App'
                                AND appsource_type = 'ctrip')
                    ) b
                    ON a.app_id = b.app_id
                WHERE a.d BETWEEN '2025-07-01' AND '2025-07-31'
                    AND DATEDIFF(a.d, from_unixtime(SUBSTRING(a.event_timestamp, 1, 10))) = 0
                    AND a.page_duration > 0
                    AND a.page_duration < 86400
                    AND a.uid IS NOT NULL
                GROUP BY a.uid, SUBSTR(a.d, 1, 7)
            ) t1
                INNER JOIN sharein_di.edw_di_dna_user_label_1013 u ON t1.uid = u.uid
        ) t2
        GROUP BY month, city
    )
)
WHERE city_rank <= 10
ORDER BY month, plat, city_rank;
```

a.d BETWEEN '2025-07-01' AND '2025-07-31'



#### 6、会员等级分布

这个分布比较好判断 我们关联1023会员等级即可

```
-- 1. 分平台统计（原逻辑保留）
SELECT month, plat, userRank, count(uid) AS user_cnt
    , ROUND(count(uid) * 1.0 / SUM(COUNT(uid)) OVER (PARTITION BY month, plat ), 3) AS ratio
FROM (
    -- 基础数据：用户-平台-月份-性别（已去重）
    SELECT a.uid, b.plat
        , SUBSTR(a.d, 1, 7) AS month
        , CASE 
            WHEN u.label_value_text = '5' THEN '白银贵宾'
            WHEN u.label_value_text = '10' THEN '黄金贵宾'
            WHEN u.label_value_text = '20' THEN '铂金贵宾'
            WHEN u.label_value_text = '30' THEN '钻石贵宾'
            WHEN u.label_value_text = '35' THEN '金钻贵宾'
            WHEN u.label_value_text = '40' THEN '黑钻贵宾'
            ELSE '普通会员'
        END AS userRank
    FROM ctripdi_prodb.cdm_ubt_pageduration_di a
        INNER JOIN (
            SELECT app_id
                , CASE 
                    WHEN app_type IN ('微信小程序', '支付宝小程序') THEN '小程序'
                    WHEN app_name = '携程旅行'
                        AND app_type = '普通App'
                        AND appsource_type = 'ctrip'
                    THEN 'Ctrip APP'
                END AS plat
            FROM ctripdi_prodb.dim_mcd_app_id_df
            WHERE app_type IN ('微信小程序', '支付宝小程序')
                OR (app_name = '携程旅行'
                    AND app_type = '普通App'
                    AND appsource_type = 'ctrip')
        ) b
        ON a.app_id = b.app_id
        INNER JOIN sharein_di.edw_di_dna_user_label_1023 u ON a.uid = u.uid
    WHERE a.d BETWEEN '2025-07-01' AND '2025-07-31'
        AND DATEDIFF(a.d, from_unixtime(SUBSTRING(a.event_timestamp, 1, 10))) = 0
        AND a.page_duration > 0
        AND a.page_duration < 86400
        AND a.uid IS NOT NULL
    GROUP BY a.uid, b.plat, SUBSTR(a.d, 1, 7), userRank
) t1
GROUP BY month, plat, userRank
UNION ALL
-- 1. 分平台统计（原逻辑保留）
SELECT month, '小程序+app' AS plat, userRank, count(uid) AS user_cnt
    , ROUND(count(uid) * 1.0 / SUM(COUNT(uid)) OVER (PARTITION BY month, plat ), 3) AS ratio
FROM (
    -- 基础数据：用户-平台-月份-性别（已去重）
    SELECT a.uid, SUBSTR(a.d, 1, 7) AS month
        , CASE 
            WHEN u.label_value_text = '5' THEN '白银贵宾'
            WHEN u.label_value_text = '10' THEN '黄金贵宾'
            WHEN u.label_value_text = '20' THEN '铂金贵宾'
            WHEN u.label_value_text = '30' THEN '钻石贵宾'
            WHEN u.label_value_text = '35' THEN '金钻贵宾'
            WHEN u.label_value_text = '40' THEN '黑钻贵宾'
            ELSE '普通会员'
        END AS userRank
    FROM ctripdi_prodb.cdm_ubt_pageduration_di a
        INNER JOIN (
            SELECT app_id
            FROM ctripdi_prodb.dim_mcd_app_id_df
            WHERE app_type IN ('微信小程序', '支付宝小程序')
                OR (app_name = '携程旅行'
                    AND app_type = '普通App'
                    AND appsource_type = 'ctrip')
        ) b
        ON a.app_id = b.app_id
        INNER JOIN sharein_di.edw_di_dna_user_label_1023 u ON a.uid = u.uid
    WHERE a.d BETWEEN '2025-07-01' AND '2025-07-31'
        AND DATEDIFF(a.d, from_unixtime(SUBSTRING(a.event_timestamp, 1, 10))) = 0
        AND a.page_duration > 0
        AND a.page_duration < 86400
        AND a.uid IS NOT NULL
    GROUP BY a.uid, SUBSTR(a.d, 1, 7), userRank
) t1
GROUP BY month, userRank
```



![image-20250927211743661](D:\编程文档\note-share\项目类\assets\image-20250927211743661.png)



#### 7、用户星级分布

```
SELECT month, plat, star, count(uid) AS user_cnt
    , ROUND(count(uid) / sum(count(uid)) OVER (PARTITION BY month, plat ), 3) AS ratio
FROM (
    SELECT t1.uid, t1.plat, t1.month, split(u.label_value_text, ',')[1] AS star
    FROM (
        -- 基础数据：用户-平台-月份-性别（已去重）
        SELECT a.uid, b.plat
            , SUBSTR(a.d, 1, 7) AS month
        FROM ctripdi_prodb.cdm_ubt_pageduration_di a
            INNER JOIN (
                SELECT app_id
                    , CASE 
                        WHEN app_type IN ('微信小程序', '支付宝小程序') THEN '小程序'
                        WHEN app_name = '携程旅行'
                            AND app_type = '普通App'
                            AND appsource_type = 'ctrip'
                        THEN 'Ctrip APP'
                    END AS plat
                FROM ctripdi_prodb.dim_mcd_app_id_df
                WHERE app_type IN ('微信小程序', '支付宝小程序')
                    OR (app_name = '携程旅行'
                        AND app_type = '普通App'
                        AND appsource_type = 'ctrip')
            ) b
            ON a.app_id = b.app_id
        WHERE a.d BETWEEN '2025-07-01' AND '2025-07-31'
            AND DATEDIFF(a.d, from_unixtime(SUBSTRING(a.event_timestamp, 1, 10))) = 0
            AND a.page_duration > 0
            AND a.page_duration < 86400
            AND a.uid IS NOT NULL
        GROUP BY a.uid, b.plat, SUBSTR(a.d, 1, 7)
    ) t1
        INNER JOIN sharein_di.edw_di_dna_user_label_1161 u ON t1.uid = u.uid
) t2
GROUP BY month, plat, star
UNION ALL
SELECT month, '小程序+app' AS plat, star, count(uid) AS user_cnt
    , ROUND(count(uid) / sum(count(uid)) OVER (PARTITION BY month ), 3) AS ratio
FROM (
    SELECT t3.uid, t3.month, split(u.label_value_text, ',')[1] AS star
    FROM (
        -- 基础数据：用户-平台-月份-性别（已去重）
        SELECT a.uid, SUBSTR(a.d, 1, 7) AS month
        FROM ctripdi_prodb.cdm_ubt_pageduration_di a
            INNER JOIN (
                SELECT app_id
                FROM ctripdi_prodb.dim_mcd_app_id_df
                WHERE app_type IN ('微信小程序', '支付宝小程序')
                    OR (app_name = '携程旅行'
                        AND app_type = '普通App'
                        AND appsource_type = 'ctrip')
            ) b
            ON a.app_id = b.app_id
        WHERE a.d BETWEEN '2025-07-01' AND '2025-07-31'
            AND DATEDIFF(a.d, from_unixtime(SUBSTRING(a.event_timestamp, 1, 10))) = 0
            AND a.page_duration > 0
            AND a.page_duration < 86400
            AND a.uid IS NOT NULL
        GROUP BY a.uid, SUBSTR(a.d, 1, 7)
    ) t3
        INNER JOIN sharein_di.edw_di_dna_user_label_1161 u ON t3.uid = u.uid
)
GROUP BY month, star
```



#### 8、性别占比

```
-- 1. 分平台统计（原逻辑保留）
SELECT month, plat, gender, user_cnt, total_user_cnt
    , ratio
FROM (
    SELECT month, plat, gender, user_cnt
        , SUM(user_cnt) OVER (PARTITION BY month, plat ) AS total_user_cnt
        , ROUND(user_cnt / SUM(user_cnt) OVER (PARTITION BY month, plat ), 4) AS ratio
    FROM (
        -- 分平台用户数统计
        SELECT month, plat, gender, COUNT(1) AS user_cnt
        FROM (
            -- 基础数据：用户-平台-月份-性别（已去重）
            SELECT a.uid, b.plat
                , SUBSTR(a.d, 1, 7) AS month
                , t2.label_value_text AS gender
            FROM ctripdi_prodb.cdm_ubt_pageduration_di a
                INNER JOIN (
                    SELECT app_id
                        , CASE 
                            WHEN app_type IN ('微信小程序', '支付宝小程序') THEN '小程序'
                            WHEN app_name = '携程旅行'
                                AND app_type = '普通App'
                                AND appsource_type = 'ctrip'
                            THEN 'Ctrip APP'
                            ELSE '-'
                        END AS plat
                    FROM ctripdi_prodb.dim_mcd_app_id_df
                    WHERE app_type IN ('微信小程序', '支付宝小程序')
                        OR (app_name = '携程旅行'
                            AND app_type = '普通App'
                            AND appsource_type = 'ctrip')
                ) b
                ON a.app_id = b.app_id
                INNER JOIN sharein_di.edw_di_dna_user_label_1066 t2 ON a.uid = t2.uid
            WHERE a.d BETWEEN '2025-08-01' AND '2025-08-31'
                AND DATEDIFF(a.d, from_unixtime(SUBSTRING(a.event_timestamp, 1, 10))) = 0
                AND a.page_duration > 0
                AND a.page_duration < 86400
                AND a.uid IS NOT NULL
            GROUP BY a.uid, b.plat, SUBSTR(a.d, 1, 7), t2.label_value_text
        ) t1
        GROUP BY month, plat, gender
    ) t2
) t3
WHERE plat != '-'
UNION ALL
SELECT month, '小程序+App' AS plat 
    , gender, user_cnt, total_user_cnt, ratio
FROM (
    SELECT month, gender, user_cnt
        , SUM(user_cnt) OVER (PARTITION BY month ) AS total_user_cnt 
        , ROUND(user_cnt / SUM(user_cnt) OVER (PARTITION BY month ), 4) AS ratio
    FROM (
        SELECT month, gender, COUNT(uid) AS user_cnt
        FROM (
            SELECT a.uid, SUBSTR(a.d, 1, 7) AS month
                , t2.label_value_text AS gender
            FROM ctripdi_prodb.cdm_ubt_pageduration_di a
                INNER JOIN (
                    SELECT app_id
                    FROM ctripdi_prodb.dim_mcd_app_id_df
                    WHERE app_type IN ('微信小程序', '支付宝小程序')
                        OR (app_name = '携程旅行'
                            AND app_type = '普通App'
                            AND appsource_type = 'ctrip')
                ) b
                ON a.app_id = b.app_id
                INNER JOIN sharein_di.edw_di_dna_user_label_1066 t2 ON a.uid = t2.uid
            WHERE a.d BETWEEN '2025-08-01' AND '2025-08-31'
                AND DATEDIFF(a.d, from_unixtime(SUBSTRING(a.event_timestamp, 1, 10))) = 0
                AND a.page_duration > 0
                AND a.page_duration < 86400
                AND a.uid IS NOT NULL
            GROUP BY a.uid, SUBSTR(a.d, 1, 7), t2.label_value_text
        ) t4
        GROUP BY month, gender
    ) t5
) t6
ORDER BY month, plat, gender;-- 统一排序

```



结果：

![image-20250927214806243](D:\编程文档\note-share\项目类\assets\image-20250927214806243.png)

明显可以发现，连续inner join的性能很差

相反先分组过滤再join画像表 性能很好 和执行计划是有关系的



#### 9、月人均使用次数

月访问次数（sid汇总）/月活（uid汇总）



count

group by month plat sid



count

group by month plat uid



两种表join-->相除即可



其实还有一种简单的写法是

按month+plat+uid+sid去分组

然后再group by month plat 然后count distinct uid即可



那么月人均使用次数 是不是就是人数/总访问数量 



```
SELECT month, plat, count(DISTINCT uid) AS mau
    , sum(s_num) AS msession_sum
    , Round(sum(s_num) * 1.0 / count(DISTINCT uid), 1) AS avg_use_cnt
FROM (
    SELECT a.uid, b.plat, a.d
        , SUBSTR(a.d, 1, 7) AS month
        , count(DISTINCT a.sid) AS s_num
    FROM ctripdi_prodb.cdm_ubt_pageduration_di a
        INNER JOIN (
            SELECT app_id
                , CASE 
                    WHEN app_type IN ('微信小程序', '支付宝小程序') THEN '小程序'
                    WHEN app_name = '携程旅行'
                        AND app_type = '普通App'
                        AND appsource_type = 'ctrip'
                    THEN 'Ctrip APP'
                    ELSE '-'
                END AS plat
            FROM ctripdi_prodb.dim_mcd_app_id_df
            WHERE app_type IN ('微信小程序', '支付宝小程序')
                OR (app_name = '携程旅行'
                    AND app_type = '普通App'
                    AND appsource_type = 'ctrip')
        ) b
        ON a.app_id = b.app_id
    WHERE a.d BETWEEN '2024-08-01' AND '2024-08-31'
        AND DATEDIFF(a.d, from_unixtime(SUBSTRING(a.event_timestamp, 1, 10))) = 0
        AND a.page_duration > 0
        AND a.page_duration < 86400
        AND a.uid IS NOT NULL
        AND a.sid IS NOT NULL -- 过滤sid缺失的记录
        AND b.plat != '-'
    GROUP BY a.uid, b.plat, a.d, SUBSTR(a.d, 1, 7)
) t1
GROUP BY month, plat
UNION ALL
SELECT month, '小程序+app' AS plat, count(DISTINCT uid) AS mau
    , sum(s_num) AS msession_sum
    , Round(sum(s_num) * 1.0 / count(DISTINCT uid), 1) AS avg_use_cnt
FROM (
    SELECT a.uid, a.d
        , SUBSTR(a.d, 1, 7) AS month
        , count(DISTINCT a.sid) AS s_num
    FROM ctripdi_prodb.cdm_ubt_pageduration_di a
        INNER JOIN (
            SELECT app_id
            FROM ctripdi_prodb.dim_mcd_app_id_df
            WHERE app_type IN ('微信小程序', '支付宝小程序')
                OR (app_name = '携程旅行'
                    AND app_type = '普通App'
                    AND appsource_type = 'ctrip')
        ) b
        ON a.app_id = b.app_id
    WHERE a.d BETWEEN '2024-08-01' AND '2024-08-31'
        AND DATEDIFF(a.d, from_unixtime(SUBSTRING(a.event_timestamp, 1, 10))) = 0
        AND a.page_duration > 0
        AND a.page_duration < 86400
        AND a.uid IS NOT NULL
        AND a.sid IS NOT NULL -- 过滤sid缺失的记录
    GROUP BY a.uid, a.d, SUBSTR(a.d, 1, 7)
) t1
GROUP BY month
```

**这里要特别注意如何计算月访问次数**

先group by uid d month plat  + count distinct sid as s_num（得到了某个人某天在某平台的访问次数）

然后再group by month plat count(uid)得到某个平台的月活 再sum(s_num)得到了所有人在某平台某个月的访问次数

如果你这么做了千万要注意：在外层计算mau的时候 就需要对uid做distinct了

因为你的明细数据时到天的，然后你直接月维度count的话 如果某个人访问了五天 会被count五次的



![image-20250927230215010](D:\编程文档\note-share\项目类\assets\image-20250927230215010.png)



#### 10、人均单日使用次数

会话次数/人数/天数: 先计算日会话count /* 日活 然后再avg即可

count(distinct sid) as s_num

group by uid d month plat



count(uid),sum(s_num),count(uid)/sum(s_num) avg_daily 单日人均使用次数

group by d month plat



AVG(avg_daily ) 即可

group by month plat



![image-20250927233254952](D:\编程文档\note-share\项目类\assets\image-20250927233254952.png)



#### 11、月人均使用时长

即计算出整个月整体停留时长/mau即可

page_duration该字段为停留时长

其实这个人均使用时长和次数很像 直接在原有sql上改即可

```
SELECT month, plat, count(DISTINCT uid) AS mau
    , sum(s_num) AS msession_sum, sum(page_duration) AS total_page_duration
    , Round(sum(s_num) * 1.0 / count(DISTINCT uid), 1) AS avg_use_cnt
    , Round(sum(page_duration) / 60 / count(DISTINCT uid), 1) AS avg_use_duration
FROM (
    SELECT a.uid, b.plat, a.d
        , SUBSTR(a.d, 1, 7) AS month
        , count(DISTINCT a.sid) AS s_num, sum(a.page_duration) AS page_duration
    FROM ctripdi_prodb.cdm_ubt_pageduration_di a
        INNER JOIN (
            SELECT app_id
                , CASE 
                    WHEN app_type IN ('微信小程序', '支付宝小程序') THEN '小程序'
                    WHEN app_name = '携程旅行'
                        AND app_type = '普通App'
                        AND appsource_type = 'ctrip'
                    THEN 'Ctrip APP'
                    ELSE '-'
                END AS plat
            FROM ctripdi_prodb.dim_mcd_app_id_df
            WHERE app_type IN ('微信小程序', '支付宝小程序')
                OR (app_name = '携程旅行'
                    AND app_type = '普通App'
                    AND appsource_type = 'ctrip')
        ) b
        ON a.app_id = b.app_id
    WHERE a.d BETWEEN '2024-08-01' AND '2024-08-31'
        AND DATEDIFF(a.d, from_unixtime(SUBSTRING(a.event_timestamp, 1, 10))) = 0
        AND a.page_duration > 0
        AND a.page_duration < 86400
        AND a.uid IS NOT NULL
        AND a.sid IS NOT NULL -- 过滤sid缺失的记录
        AND b.plat != '-'
    GROUP BY a.uid, b.plat, a.d, SUBSTR(a.d, 1, 7)
) t1
GROUP BY month, plat
UNION ALL
SELECT month, '小程序+app' AS plat, count(DISTINCT uid) AS mau
    , sum(s_num) AS msession_sum
    , sum(a.page_duration) / 60 AS page_duration
    , Round(sum(s_num) * 1.0 / count(DISTINCT uid), 1) AS avg_use_cnt
    , Round(sum(page_duration) / 60 / count(DISTINCT uid), 1) AS avg_use_duration
FROM (
    SELECT a.uid, a.d
        , SUBSTR(a.d, 1, 7) AS month
        , count(DISTINCT a.sid) AS s_num, sum(a.page_duration) AS page_duration
    FROM ctripdi_prodb.cdm_ubt_pageduration_di a
        INNER JOIN (
            SELECT app_id
            FROM ctripdi_prodb.dim_mcd_app_id_df
            WHERE app_type IN ('微信小程序', '支付宝小程序')
                OR (app_name = '携程旅行'
                    AND app_type = '普通App'
                    AND appsource_type = 'ctrip')
        ) b
        ON a.app_id = b.app_id
    WHERE a.d BETWEEN '2024-08-01' AND '2024-08-31'
        AND DATEDIFF(a.d, from_unixtime(SUBSTRING(a.event_timestamp, 1, 10))) = 0
        AND a.page_duration > 0
        AND a.page_duration < 86400
        AND a.uid IS NOT NULL
        AND a.sid IS NOT NULL -- 过滤sid缺失的记录
    GROUP BY a.uid, a.d, SUBSTR(a.d, 1, 7)
) t1
GROUP BY month
```



#### 12、人均单日使用时长

这个和人均单次使用次数也很像



```
SELECT month, plat
    , Round(AVG(daily_cnt), 1) AS avg_use_cnt
    , Round(AVG(daily_duration), 1) AS avg_use_duration
FROM (
    SELECT month, plat, d, count(uid) AS daily_u_cnt
        , sum(s_num) AS daily_s_sum, sum(page_duration) AS page_duration_sum
        , sum(s_num) / count(uid) AS daily_cnt
        , sum(page_duration) / 60 / count(uid) AS daily_duration
    FROM (
        SELECT a.uid, b.plat, a.d
            , SUBSTR(a.d, 1, 7) AS month
            , count(DISTINCT a.sid) AS s_num, sum(a.page_duration) AS page_duration
        FROM ctripdi_prodb.cdm_ubt_pageduration_di a
            INNER JOIN (
                SELECT app_id
                    , CASE 
                        WHEN app_type IN ('微信小程序', '支付宝小程序') THEN '小程序'
                        WHEN app_name = '携程旅行'
                            AND app_type = '普通App'
                            AND appsource_type = 'ctrip'
                        THEN 'Ctrip APP'
                        ELSE '-'
                    END AS plat
                FROM ctripdi_prodb.dim_mcd_app_id_df
                WHERE app_type IN ('微信小程序', '支付宝小程序')
                    OR (app_name = '携程旅行'
                        AND app_type = '普通App'
                        AND appsource_type = 'ctrip')
            ) b
            ON a.app_id = b.app_id
        WHERE a.d BETWEEN '2024-08-01' AND '2024-08-31'
            AND DATEDIFF(a.d, from_unixtime(SUBSTRING(a.event_timestamp, 1, 10))) = 0
            AND a.page_duration > 0
            AND a.page_duration < 86400
            AND a.uid IS NOT NULL
            AND a.sid IS NOT NULL -- 过滤sid缺失的记录
            AND b.plat != '-'
        GROUP BY a.uid, b.plat, a.d, SUBSTR(a.d, 1, 7)
    ) t1
    GROUP BY d, month, plat
) t2
GROUP BY month, plat
UNION ALL
SELECT month, '小程序+app' AS plat
    , Round(AVG(daily_cnt), 1) AS avg_use_cnt
    , Round(AVG(daily_duration), 1) AS avg_use_duration
FROM (
    SELECT month, d, count(uid) AS daily_u_cnt
        , sum(s_num) AS daily_s_sum, sum(page_duration) AS page_duration_sum
        , sum(s_num) / count(uid) AS daily_cnt
        , sum(page_duration) / 60 / count(uid) AS daily_duration
    FROM (
        SELECT a.uid, a.d
            , SUBSTR(a.d, 1, 7) AS month
            , count(DISTINCT a.sid) AS s_num, sum(a.page_duration) AS page_duration
        FROM ctripdi_prodb.cdm_ubt_pageduration_di a
            INNER JOIN (
                SELECT app_id
                FROM ctripdi_prodb.dim_mcd_app_id_df
                WHERE app_type IN ('微信小程序', '支付宝小程序')
                    OR (app_name = '携程旅行'
                        AND app_type = '普通App'
                        AND appsource_type = 'ctrip')
            ) b
            ON a.app_id = b.app_id
        WHERE a.d BETWEEN '2024-08-01' AND '2024-08-31'
            AND DATEDIFF(a.d, from_unixtime(SUBSTRING(a.event_timestamp, 1, 10))) = 0
            AND a.page_duration > 0
            AND a.page_duration < 86400
            AND a.uid IS NOT NULL
            AND a.sid IS NOT NULL -- 过滤sid缺失的记录
        GROUP BY a.uid, a.d, SUBSTR(a.d, 1, 7)
    ) t1
    GROUP BY d, month
) t2
GROUP BY month
```



![image-20250928001414657](D:\编程文档\note-share\项目类\assets\image-20250928001414657.png)



#### 13、人均使用天数

SUM（每人使用天数）/人数




group by d month plat uid 拿到明细



group by month plat uid --- count(d) as d_cnt 得到了某个uid的使用天数



group by month plat --- sum(d_cnt) /count(uid)即可得到人均使用天数



```
SELECT month, plat, count(uid) AS mau
    , sum(d_cnt) AS sum_d_cnt
    , Round(sum(d_cnt) / count(uid), 1) AS avg_use_days
FROM (
    SELECT month, plat, uid, count(d) AS d_cnt
    FROM (
        SELECT a.uid, b.plat, a.d
            , SUBSTR(a.d, 1, 7) AS month
        FROM ctripdi_prodb.cdm_ubt_pageduration_di a
            INNER JOIN (
                SELECT app_id
                    , CASE 
                        WHEN app_type IN ('微信小程序', '支付宝小程序') THEN '小程序'
                        WHEN app_name = '携程旅行'
                            AND app_type = '普通App'
                            AND appsource_type = 'ctrip'
                        THEN 'Ctrip APP'
                        ELSE '-'
                    END AS plat
                FROM ctripdi_prodb.dim_mcd_app_id_df
                WHERE app_type IN ('微信小程序', '支付宝小程序')
                    OR (app_name = '携程旅行'
                        AND app_type = '普通App'
                        AND appsource_type = 'ctrip')
            ) b
            ON a.app_id = b.app_id
        WHERE a.d BETWEEN '2024-08-01' AND '2024-08-31'
            AND DATEDIFF(a.d, from_unixtime(SUBSTRING(a.event_timestamp, 1, 10))) = 0
            AND a.page_duration > 0
            AND a.page_duration < 86400
            AND a.uid IS NOT NULL
            AND b.plat != '-'
        GROUP BY a.uid, b.plat, a.d, SUBSTR(a.d, 1, 7)
    ) t1
    GROUP BY month, plat, uid
) t2
GROUP BY month, plat
UNION ALL
SELECT month, '小程序+app' AS plat, count(uid) AS mau
    , sum(d_cnt) AS sum_d_cnt
    , Round(sum(d_cnt) / count(uid), 1) AS avg_use_days
FROM (
    SELECT month, uid, count(d) AS d_cnt
    FROM (
        SELECT a.uid, a.d
            , SUBSTR(a.d, 1, 7) AS month
        FROM ctripdi_prodb.cdm_ubt_pageduration_di a
            INNER JOIN (
                SELECT app_id
                FROM ctripdi_prodb.dim_mcd_app_id_df
                WHERE app_type IN ('微信小程序', '支付宝小程序')
                    OR (app_name = '携程旅行'
                        AND app_type = '普通App'
                        AND appsource_type = 'ctrip')
            ) b
            ON a.app_id = b.app_id
        WHERE a.d BETWEEN '2024-08-01' AND '2024-08-31'
            AND DATEDIFF(a.d, from_unixtime(SUBSTRING(a.event_timestamp, 1, 10))) = 0
            AND a.page_duration > 0
            AND a.page_duration < 86400
            AND a.uid IS NOT NULL
        GROUP BY a.uid, a.d, SUBSTR(a.d, 1, 7)
    ) t1
    GROUP BY month, uid
) t2
GROUP BY month
```



![image-20250928004037985](D:\编程文档\note-share\项目类\assets\image-20250928004037985.png)

对数成功

#### 14、人均单次使用时长

这个是和人家单日使用时长差不多

我们单日的计算是：

我们先算出单日一共停留多久，然后单日有多少次访问，除完后AVG即可

sum(duration),count(distinct sid)

group by d month plat

avg即可

group by month,plat



```
SELECT month, plat
    , Round(avg(sing_duration), 1) AS avg_sing_duration
FROM (
    SELECT b.plat, a.d
        , SUBSTR(a.d, 1, 7) AS month
        , count(DISTINCT a.sid) AS s_num, sum(a.page_duration) AS page_duration
        , sum(a.page_duration) / 60 / count(DISTINCT a.sid) AS sing_duration
    FROM ctripdi_prodb.cdm_ubt_pageduration_di a
        INNER JOIN (
            SELECT app_id
                , CASE 
                    WHEN app_type IN ('微信小程序', '支付宝小程序') THEN '小程序'
                    WHEN app_name = '携程旅行'
                        AND app_type = '普通App'
                        AND appsource_type = 'ctrip'
                    THEN 'Ctrip APP'
                    ELSE '-'
                END AS plat
            FROM ctripdi_prodb.dim_mcd_app_id_df
            WHERE app_type IN ('微信小程序', '支付宝小程序')
                OR (app_name = '携程旅行'
                    AND app_type = '普通App'
                    AND appsource_type = 'ctrip')
        ) b
        ON a.app_id = b.app_id
    WHERE a.d BETWEEN '2024-08-01' AND '2024-08-31'
        AND DATEDIFF(a.d, from_unixtime(SUBSTRING(a.event_timestamp, 1, 10))) = 0
        AND a.page_duration > 0
        AND a.page_duration < 86400
        AND a.uid IS NOT NULL
        AND a.sid IS NOT NULL -- 过滤sid缺失的记录
        AND b.plat != '-'
    GROUP BY b.plat, a.d, SUBSTR(a.d, 1, 7)
) t1
GROUP BY month, plat
UNION ALL
SELECT month, '小程序+app' AS plat
    , Round(avg(sing_duration), 1) AS avg_sing_duration
FROM (
    SELECT a.d, SUBSTR(a.d, 1, 7) AS month
        , count(DISTINCT a.sid) AS s_num, sum(a.page_duration) AS page_duration
        , sum(a.page_duration) / 60 / count(DISTINCT a.sid) AS sing_duration
    FROM ctripdi_prodb.cdm_ubt_pageduration_di a
        INNER JOIN (
            SELECT app_id
            FROM ctripdi_prodb.dim_mcd_app_id_df
            WHERE app_type IN ('微信小程序', '支付宝小程序')
                OR (app_name = '携程旅行'
                    AND app_type = '普通App'
                    AND appsource_type = 'ctrip')
        ) b
        ON a.app_id = b.app_id
    WHERE a.d BETWEEN '2024-08-01' AND '2024-08-31'
        AND DATEDIFF(a.d, from_unixtime(SUBSTRING(a.event_timestamp, 1, 10))) = 0
        AND a.page_duration > 0
        AND a.page_duration < 86400
        AND a.uid IS NOT NULL
        AND a.sid IS NOT NULL -- 过滤sid缺失的记录
    GROUP BY a.d, SUBSTR(a.d, 1, 7)
) t1
GROUP BY month
```



![image-20250928010724325](D:\编程文档\note-share\项目类\assets\image-20250928010724325.png)

这个跑出来有点问题

![image-20250928013729602](D:\编程文档\note-share\项目类\assets\image-20250928013729602.png)

跑完发现还是有点差距



#### 15、月访问天数分布

1

[2,3]

[4,7]

[8,31]



先计算用户在平台的访问天数



group by d month uid plat



case when --> d_num_range

group by month uid plat --> count(d)  as d_num







group by month plat d_num_range --> 

count(uid) -->得到了该访问天数的人数+ count()/其他区间 得到一个分布比例：这里可以不做小数点

![image-20250928013632703](D:\编程文档\note-share\项目类\assets\image-20250928013632703.png)





### 指标建设-留存类

先拿到明细表：

天 uid

天 uid+plat











































### 指标建设-交易类

**小结**

我发现对hive sql的理解还是比较差，语法都不太熟练，更别提执行机会做优化了

此外沟通上面 其实我应该合理评估后及时提出风险

因为考虑到业务交付，我要担业务风险的，是应该及时说明的，这是沟通上的问题

综上：这次失误暴露出两点：

1、在数仓能力胜任上存在明显缺陷



### 分析job

首先通过看板我们可以看到sql预览，发现nova看板就是两张表：

- qa_db.adm_sci_nps_mau_ditribution_di
- qa_db.adm_sci_nps_ubt_trd_mi

分成了整体大盘数据指标，和其他占比表

首先这个设计就很值得学习：将指标分成月维度|月维度+占比维度

从而形成两张表



我们先看一下adm_sci_nps_ubt_trd_mi这张大盘的一些指标



发现这张表有个上游：cdm_sci_prj_nps_ubt_mau_mi

dim_hoteldb.dimcity_source t6 
    on t5.label_value_text = t6.cityid
    AND t6.d = (select max(d) FROM dim_hoteldb.dimcity_source)

这里要限制一下currentflag='T'

调度：秒 分 时 日 月 星期

按月分区



然后我们看下怎么计算月维度指标的：

这里有个写法：

```
SELECT m,'ALL' plat,uid ,MAX(is_new) as is_new
			FROM qa_db.cdm_sci_prj_nps_ubt_mau_mi
			WHERE m='${zdt.addDay(-1).format("yyyy-MM")}'
			GROUP BY 1,2,3 
```

解读一下coalesce(t2.d,t1.d)这个函数：SQL中常用的空值处理函数，核心作用是从多个参数中返回第一个非NULL的值，如果所有参数都为NULL最终返回也为NULL



发现一张交易表：mid_sci_input_ord_df（视图）













# 数仓理论

### 分层

Data WareHouse数据仓库分层是一种将数据从原始状态逐步转化为可用信息的结构化设计方法，核心目标是将数据从原始素材变成可用资产

目前行业主流的数仓分层以“原始数据-->清洗整合--->按需服务”为脉络，最经典的是三层模型

1、数据接入层 ODS

数仓的原始数据缓冲区直接对接业务系统（如电商订单系统、支付系统），完全或近似保留原始数据不做过多加工

隔离业务系统与数仓



2、数据仓库层DW

数仓的核心加工层，对ODS层数据进行清洗、整合、建模，形成结构化的 “数据资产库”

根据加工粒度和用途，进一步细分为DWD明细层、DWM中间层、DWS汇总层



3、数据服务层ADS

数仓的前端出口直接为业务应用提供即用型数据，统一数据出口



### SQL执行

首先理解SQL语法的逻辑顺序（写SQL时的语句顺序）和数据库的实际执行顺序（优化器解析后的执行步骤）

1、逻辑顺序

通常写sql的时候：select from where groupby having order by的语法顺序

但数据库例如hive、mysql的优化器会根据执行计划调整实际步骤，目的是减少参与计算的数据量

2、执行顺序

数据库典型的实际执行顺序是：

- FROM 先定位要查询的表
- JOIN 关联其他表
- WHERE 过滤行级数据
- GROUP BY按指定字段分组
- 聚合函数 对分组后的数据计算指标
- HAVING 过滤分组级数据
- SELECT 提取最终需要的字段
- ORDER BY/LIMIT 排序或限制结果行数



### Join

Join操作通过关联条件将两个表中符合条件的记录组合成新纪录，核心逻辑分为“准备”、关联、优化三个阶段

- **驱动表（Driving Table）**：作为关联的 “基准表”，先被扫描，其结果会用来匹配另一张表。通常选择数据量较小的表作为驱动表，减少后续匹配次数。
- **被驱动表（Driven Table）**：被匹配的表，会根据驱动表的结果进行查询和关联。
- **关联条件（Join Condition）**：如`a.id = b.id`，用于判断两表记录是否需要组合。

通常小表作为驱动表



一般根据数据量和底层引擎，JOIN会采用不同的算法，核心有三种：

1、嵌套循环连接

场景：驱动表极小（如几千行之内）被驱动表有索引

逻辑：遍历驱动表，每条记录用关联关系去被驱动表中查询匹配的记录，如果匹配组合成新记录输出

2、哈希连接hash join--大数据场景常用

场景：两表数据量大，且无合适索引

逻辑：遍历驱动表根据关联建计算哈希值，将<key,记录>存储到内存哈希表

遍历被驱动表，对每条记录的关联键计算哈希值，到哈希表中查询相同哈希值记录（若哈希冲突再校验值相等）匹配成功后输出

优势：时间复杂度低 相比嵌套来说从O（N*M）变成O（n+m） 相当于空间换时间

3、排序合并连接

场景：两表数据量大，且关联键已排序

逻辑：将两张表根据关键字排序，同时遍历有点类似归并排序

好处是可以避免哈希表的内存限制



### Corn表达式

常用于定时任务调度，由6个空格分隔的字段组成（部分场景包含第七位年份），从左到右对应

秒 分 时 日 月 星期

例如0 58 11 * * ？

即11:58分0秒 每个月的每天 都调度 其中星期为占位符

因为第四位日和第六位星期是互斥字段，若通过日字段指定了具体日期，则星期字段必须用？占位表示不限制

反之若知道了具体星期则日字段必须用？





INSERT OVERWRITE TABLE ctripdi_prodb.mid_dna_user_label_initial_8735





INSERT OVERWRITE TABLE ubt_search.cdm_ubt_performance_metrics_table PARTITION (d='${hivevar:dt}')



INSERT OVERWRITE TABLE ubt_search.cdm_ubt_performance_metrics_table PARTITION (d='${hivevar:dt}')



































